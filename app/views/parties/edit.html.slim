h1 修改關鍵字

= simple_form_for(@party, html: {class: 'form-horizontal center' }) do |f|
  = render :partial => 'form', :locals => { :f => f }

= link_to '查看', party_path(@party), class: "btn"
= link_to '回到列表', parties_path, class: "btn"
